// TODO: Type checking, etc

export type RegisterRequest = {
    user_name: string,
    password: string,
    name: string
  };

  export type LoginRequest = {
    user_name: string,
    password: string,
  };
  
